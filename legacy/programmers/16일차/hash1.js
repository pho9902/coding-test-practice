const s = "foo foo foo foo";
const s2 = "foo bar bar foo";
const pattern = "abba";

const solution = (pattern, s) => {
  const str = s.split(" ");
  const pat = pattern.split("");
};

console.log(solution(pattern, s));
